# Общий frontend Tubi

## Языки

Общий интерфейс для обоих frontend-модулей. Рабочая ветка: `feature/business-ui`; в ней объединён готовый backend из `feature/backend-quality`.

В компонентах marketplace:

```tsx
import { useLocale, type Locale } from "../../shared/i18n";
import { messages } from "./locales";

const { locale, setLocale } = useLocale();
const copy = messages[locale];
```

- `Locale = "ru" | "kk"` согласно текущему заданию. Старое сохранённое значение `en` даёт русский язык по умолчанию.
- Provider не нужен. Выбор хранится в `localStorage["tubi.locale"]`, при недоступном хранилище — в памяти.
- Переключатель уже находится в App. Не меняйте React key формы при смене языка.
- Словари marketplace храните в `features/marketplace/locales`, бизнес-словарь находится в `business/locales`.
- Переводите подписи и состояния, сохраняйте пользовательские строки.
- `request/json` из shared/http остаются совместимыми. HTTP-клиент сам добавляет `Accept-Language: ru | kk` по текущему выбору.
- `ApiError` содержит code, status, fields и сообщение сервера с языком запроса; `errorMessage(error, locale)` показывает его на исходном языке либо локализованную общую ошибку после смены языка.
- Интегрированный backend поддерживает Accept-Language для live/fallback вопросов, предупреждений, ошибок и рейтинга. Уже полученные вопросы и пользовательские ответы не переводятся повторным запросом при смене языка. Всегда отображайте `warnings`, включая режим live.
- Существующие exports shared/constants сохранены для совместимости. Их русские подписи можно постепенно заменить локальным словарём marketplace.

## Общие стили

`shared/styles.css` подключён один раз в main.tsx.

Токены: `--background`, `--surface`, `--ink`, `--muted`, `--accent`, `--line`.
Для видимых границ полей используйте `--control-border`: светлый `--line` предназначен для разделителей.
Старые `--purple` и `--purple-dark` сохранены как aliases акцента.

Общие классы: `button primary`, `button secondary`, `text-button`, `panel`, `field-hint`, `notice`, `error-box`, `sr-only`.
Размер текста формы 16px, область основных кнопок не меньше 44px, контур клавиатурного фокуса видимый.
Каждому input/textarea/select нужны label и стабильный id. Подсказку связывайте через aria-describedby.

## Тесты

```sh
cd web
npm ci
npm test
npm run build
```

Общая конфигурация: vitest.config.ts, jsdom, React Testing Library и user-event.
Setup shared/test/setup.ts подключает jest-dom и очистку DOM после каждого теста.
Тесты размещайте рядом с модулем в *.test.ts или *.test.tsx; новую конфигурацию создавать не нужно.
Node соответствует README: 22.12+. jsdom закреплён на совместимой ветке 26.
Vite использует configLoader runner, чтобы не собирать конфигурацию через esbuild в ограниченной Windows-среде.

## Бизнес-сценарий

useBusinessForm отвечает за состояние и жизненный цикл запросов; api.ts — за HTTP; компоненты — за отображение.
Рейтинг полностью приходит с сервера. ratingAdvice использует только разницу max − earned из breakdown.
Ответы хранятся при возврате и ошибках; compose не удаляет исходные вопросы.
Изменение карточки снимает подтверждение, а публикация выполняется после отдельного сохранения.
Устаревший evaluate блокируется счётчиком версии и AbortController.

## Границы интеграции

Каталог, карточки и отклики используют `marketplace/locales/index.ts`, а отзывы — `marketplace/locales/review.ts`. Все экраны подключены к `useLocale` и общим тёплым токенам. Декоративный баннер и дублирующий CTA удалены. Успешные мутации отображаются отдельно от ошибки обновления списка; повтор обновления не повторяет сохранение.

Для приёмки: `npm test`, `npm run build`, backend unittest. Проверить описание → три вопроса → карточка → рейтинг → подтверждение → сохранение → публикация → отклик → ручной выбор. Автоматические frontend-тесты подменяют HTTP; тесты backend используют временную SQLite и подмену OpenAI. Не считать эти проверки подтверждением доступности настоящего провайдера.

Повтор публикации использует сохранённый ID карточки. Если ответ успешного создания задачи потеряется в сети, повтор создания может дать дубль: серверного ключа идемпотентности в текущем контракте нет.

## Результат совместной проверки — 23 сентября 2026

- `npm test`: 37 тестов в 7 файлах, все прошли. Включён сценарий App в StrictMode: создание → сохранение → ошибка публикации → повтор с прежним ID → открытие карточки.
- `npm run build`: TypeScript и production-сборка Vite прошли.
- Backend unittest: 61 тест прошёл; временная SQLite и подменённые ответы OpenAI.
- В браузере на отдельной базе: описание → 3 вопроса → ответы → редактирование → рейтинг 55 → 80 → подтверждение → сохранение → публикация → отклик → ручной выбор команды. До выбора отклик оставался на рассмотрении.
- Дополнительно: пустое описание блокирует продолжение; RU/KK сохраняет описание и ответы; удаление данных снижает рейтинг 80 → 60 и снимает подтверждение; восстановление возвращает 80. Проверены подписи полей, фокус и управление кнопками/checkbox с клавиатуры. Ошибок JavaScript в журнале проверенного сценария нет.
- Контраст общих цветов: основной текст на фоне 13.10:1, вторичный текст на белом 5.46:1, белый на акценте 5.53:1, границы полей на белом 3.82:1. Это проверка общих токенов, не полный аудит всех экранов marketplace.
- Настоящий OpenAI API в этой проверке не вызывался: браузерный сценарий использовал явно обозначенный деморежим. Рабочая база и ключи не затрагивались.

Дополнение с отзывами: 47 frontend-тестов в 8 файлах, 69 backend-тестов и production-сборка прошли. Совместимое дополнение контракта: nullable `Proposal.review` и `POST /api/proposals/{id}/review`. Старые данные SQLite читаются без миграции.

Финальная доработка интерфейса: 58 frontend-тестов в 9 файлах, 69 backend-тестов и сборка прошли. Добавлены прямые ссылки `?task=ID`, восстановление карточки после перезагрузки, защита списка от устаревших запросов и блокировка навигации во время запроса конструктора. В браузере проверены RU/KK-каталог и восстановление карточки. Системные статусы AI не заменяют проверку реального запроса к провайдеру.

## Как обновить локальную копию

В корне чистой рабочей копии выполните:

```powershell
git fetch origin
git switch feature/business-ui
git pull --ff-only
cd web
npm.cmd ci
npm.cmd run build
cd ..
```

Остановите прежний сервер сочетанием Ctrl+C в его терминале и запустите из корня репозитория:

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir server --host 127.0.0.1 --port 8001
```

Откройте `http://127.0.0.1:8001/`. Существующий `server/.env` остаётся локальным; не добавляйте его в коммит. Для первой установки Python-зависимостей используйте README.
